<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FSPSWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FSPSWinUI"
    xmlns:vm="using:FSPSWinUI.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="15">
        <Grid.DataContext>
            <vm:MainWindowViewModel />
        </Grid.DataContext>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom Title Bar (theme-aware) -->
        <Border Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Height="48" Grid.Row="0">
                <Grid Margin="12,0">
                    <!-- Use three columns so the center column occupies ~80%: 1* | 8* | 1* -->
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="8*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <!-- Center content in the middle column (will stretch to 80% of available width) -->
                    <Grid Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ComboBox x:Name="ProfileComboBox" Grid.Column="0" ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" DisplayMemberPath="Name" MinWidth="200" HorizontalAlignment="Stretch" />

                        <Button Grid.Column="1" Margin="8,0,0,0" Click="AddProfileButton_Click" ToolTipService.ToolTip="Add profile">
                            <SymbolIcon Symbol="Add" />
                        </Button>

                        <Button Grid.Column="2" Margin="8,0,0,0" Command="{Binding EditProfileCommand}" ToolTipService.ToolTip="Edit selected profile">
                            <SymbolIcon Symbol="Edit" />
                        </Button>

                        <Button Grid.Column="3" Margin="8,0,0,0" Command="{Binding DeleteProfileCommand}" ToolTipService.ToolTip="Delete selected profile">
                            <SymbolIcon Symbol="Delete" />
                        </Button>
                    </Grid>
                </Grid>
        </Border>

        <!-- Content area -->
        <Grid Grid.Row="1">
            <!-- Placeholder for main content -->
        </Grid>
    </Grid>
</Window>
