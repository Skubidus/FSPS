<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FSPSWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FSPSWinUI"
    xmlns:vm="using:FSPSWinUI.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title bar (height set from code-behind to match system caption buttons) -->
        <Border x:Name="TitleBarBorder" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Draggable area with app icon and title (left aligned, 15px margin) -->
                <Grid x:Name="WindowDragArea" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5,0,0,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <SymbolIcon Symbol="Setting" Width="20" Height="20" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" Margin="0,0,8,0" />
                        <TextBlock Text="{x:Bind ViewModel.AppTitle}" VerticalAlignment="Center" FontSize="16" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" AutomationProperties.Name="Application Title" AutomationProperties.HeadingLevel="Level1" />
                    </StackPanel>
                </Grid>

                <!-- Right column intentionally left empty so system caption buttons can sit there -->
            </Grid>
        </Border>

        <!-- Main content area with internal padding 15 -->
        <Grid Grid.Row="1" Padding="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="8*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <!-- Center column: existing profiles control grid (will occupy ~80% via 1*/8*/1*) -->
            <Grid Grid.Column="1" x:Name="ProfilesGrid" HorizontalAlignment="Stretch" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Active Profile:" Margin="0, 0, 10, 0" VerticalAlignment="Center" />

                <ComboBox x:Name="ProfileComboBox" Grid.Column="1" ItemsSource="{x:Bind ViewModel.Profiles}" SelectedItem="{x:Bind ViewModel.SelectedProfile, Mode=TwoWay}" DisplayMemberPath="Name" MinWidth="200" HorizontalAlignment="Stretch" VerticalAlignment="Center" AutomationProperties.Name="Profile Selection" IsEnabled="{x:Bind ViewModel.HasProfiles, Mode=OneWay}" />

                <Button Grid.Column="2" Margin="8,0,0,0" Command="{x:Bind ViewModel.AddProfileCommand}" ToolTipService.ToolTip="Add profile" VerticalAlignment="Center" AutomationProperties.Name="Add Profile">
                    <SymbolIcon Symbol="Add" />
                </Button>

                <Button Grid.Column="3" Margin="8,0,0,0" Command="{x:Bind ViewModel.EditProfileCommand}" ToolTipService.ToolTip="Edit selected profile" VerticalAlignment="Center" AutomationProperties.Name="Edit Profile">
                    <SymbolIcon Symbol="Edit" />
                </Button>

                <Button Grid.Column="4" Margin="8,0,0,0" Click="DeleteProfileButton_Click" ToolTipService.ToolTip="Delete selected profile" IsEnabled="{x:Bind ViewModel.CanDelete, Mode=OneWay}" VerticalAlignment="Center" AutomationProperties.Name="Delete Profile">
                    <SymbolIcon Symbol="Delete" />
                </Button>
            </Grid>
        </Grid>

    </Grid>
</Window>
