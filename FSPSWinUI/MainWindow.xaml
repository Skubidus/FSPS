<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FSPSWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FSPSWinUI"
    xmlns:vm="using:FSPSWinUI.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title bar (height set from code-behind to match system caption buttons) -->
        <Border x:Name="TitleBarBorder" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Grid.Row="0">
            <!--{ThemeResource ApplicationPageBackgroundThemeBrush}-->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Draggable area with app title (left aligned, 15px margin) -->
                <Grid x:Name="WindowDragArea" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0,0,0">
                    <TextBlock Text="{Binding AppTitle}" VerticalAlignment="Center" FontSize="18" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" />
                </Grid>

                <!-- Right column intentionally left empty so system caption buttons can sit there -->
            </Grid>
        </Border>

        <!-- Main content area with internal padding 15 -->
        <Grid Grid.Row="1" Padding="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="8*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <!-- Center column: existing profiles control grid (will occupy ~80% via 1*/8*/1*) -->
            <Grid Grid.Column="1" x:Name="ProfilesGrid" HorizontalAlignment="Stretch" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="ProfileComboBox" Grid.Column="0" ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile, Mode=TwoWay}" DisplayMemberPath="Name" MinWidth="200" HorizontalAlignment="Stretch" />

                <Button Grid.Column="1" Margin="8,0,0,0" Click="AddProfileButton_Click" ToolTipService.ToolTip="Add profile">
                    <SymbolIcon Symbol="Add" />
                </Button>

                <Button Grid.Column="2" Margin="8,0,0,0" Command="{Binding EditProfileCommand}" ToolTipService.ToolTip="Edit selected profile">
                    <SymbolIcon Symbol="Edit" />
                </Button>

                <Button Grid.Column="3" Margin="8,0,0,0" Click="DeleteProfileButton_Click" ToolTipService.ToolTip="Delete selected profile" IsEnabled="{Binding CanDelete}">
                    <SymbolIcon Symbol="Delete" />
                </Button>
            </Grid>
        </Grid>

    </Grid>
</Window>
